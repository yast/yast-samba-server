{

// test for SambaServer::enableLogon( enable )
// $Id$
// testedfiles: Samba.ycp SambaServer.ycp

    include "testsuite.ycp";
    
    import "Samba";

    list rwe = [
            $[
                "target" : $[
                    "tmpdir" : "/tmp",
                    "size" : 0
                ]
            ],
            $[],
            $[
                "target" : $[
                    "bash_output" : $[]
                ]
            ]
        ];
    TESTSUITE_INIT(rwe, nil);
    import "SambaServer";
    
    TEST( ``(SambaServer::shares), rwe, nil);
    TEST( ``(SambaServer::global_config["domain logons"]:nil), rwe, nil);
    TEST( ``(SambaServer::modified), rwe, nil);
    
    Samba::modified = false;
    TEST( ``( SambaServer::enableNetlogon(true) ), rwe, nil);

    TEST( ``(SambaServer::shares), rwe, nil);
    TEST( ``(SambaServer::global_config["domain logons"]:nil), rwe, nil);
    TEST( ``(SambaServer::modified), rwe, nil);

    TEST( ``( SambaServer::enableNetlogon(false) ), rwe, nil);

    TEST( ``(SambaServer::shares), rwe, nil);
    TEST( ``(SambaServer::global_config["domain logons"]:nil), rwe, nil);
    TEST( ``(SambaServer::modified), rwe, nil);
}
