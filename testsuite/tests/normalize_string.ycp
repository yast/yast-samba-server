{

// test for normalize_string
// $Id$

    include "testsuite.ycp";
    
    include "samba-server/routines.ycp";

    list rwe = [];
    TEST( ``( normalize_string( "yes", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "yEs", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "1", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "true", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "TRue", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "no", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "0", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "FALSE", nil ) ), rwe, nil);
    TEST( ``( normalize_string( "auto", nil ) ), rwe, nil);
    TEST( ``( normalize_string( nil, "true" ) ), rwe, nil);
}
