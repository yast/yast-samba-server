{

// test for normalize_boolean
// $Id$

    include "testsuite.ycp";
    
    include "samba-server/routines.ycp";

    list rwe = [];
    TEST( ``( normalize_boolean( "yes", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "yEs", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "1", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "true", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "TRue", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "no", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "0", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( "FALSE", nil ) ), rwe, nil);
    TEST( ``( normalize_boolean( nil, true ) ), rwe, nil);
}
